-----------   daily motion api wrapper to build custom api  ---------

npm init 
-will generate package.json.
-not required to install because it is alread in the daily-motion-api folder

Install npm to run
npm install (it will generate the node_modules & package-lock-jason file in directory)



Dependencies Installation Required:

npm install --save superagent
